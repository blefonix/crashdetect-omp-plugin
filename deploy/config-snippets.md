# Server config snippets for crashdetect

## Dev / Stage (recommended)

Enable `crashdetect` near the top of plugin list.

## Prod (default policy)

Keep disabled unless debugging an incident.

## Prod (incident mode)

Temporarily enable with low-noise log settings, then disable after fix.
